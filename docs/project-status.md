# 프로젝트 상태

이 문서는 현재 기본 브랜치의 구현 상태와 다음 작업의 우선순위를 요약합니다.
세부 결함은 [알려진 문제](known-issues.md), 중장기 방향은
[로드맵](roadmap.md), 서버의 현재 동작은 [서버 구조](architecture.md)를
참고합니다.

마지막 갱신: 2026-09-08

## 완료된 기반

- C++20 기반 프로토콜, 서버 코어와 Linux 네트워크 계층 분리
- `epoll`과 `eventfd`를 사용하는 단일 I/O 스레드 서버
- worker pool과 세션별 이벤트 순서 보장
- 로그인, 방 생성·참가·퇴장, 채팅과 위치 broadcast
- 입력·출력 queue, 세션 pending write와 동시 세션 수 제한
- high/low watermark 기반 읽기 흐름 제어와 느린 클라이언트 격리
- 정상 종료 drain 상태 머신과 과부하 통계
- 실제 서버의 broadcast, 다중 방과 slow-client 부하 시나리오 실행기
- 실행 환경, 실패·누락 수와 p50/p95/p99를 포함하는 반복 측정 결과
- GoogleTest, Google Benchmark, ASan/UBSan과 플랫폼별 CI 기반
- Qt 6 Widgets 클라이언트의 application, network, UI 모듈 분리
- Qt 클라이언트의 연결, 로그인, 방 작업과 채팅 UI
- PostgreSQL 영구 사용자 UUID와 같은 이름 재접속 복구
- bounded DB executor와 deferred handler completion 기반
- NDJSON 운영 로그와 주기·최종 과부하 통계 외부 노출
- 연결별 프로토콜 버전 협상과 구버전 연결 거절
- 공용 structured payload codec과 RFC 3629 UTF-8 검증
- 이름·방 32바이트와 채팅 1291바이트 고정 상한

## 최근 완료

- 2026-09-08: 외부 다중 방의 약 40ms 전송 대기를 패킷·TCP 설정 대조로 분석.
  RoomService 잠금의 대기·보유 시간과
  [지연 분석 결과](performance/2026-09-08-latency-analysis/README.md) 기록.
  운영 TCP 기본값 변경과 비용·원격 환경 검증은 후속 작업

- 2026-09-08: 외부 다중 방의 클라이언트 종료와 늦은 퇴장 알림이 겹쳐
  RST/EPIPE가 발생하는 경로를 원래 부하 조건에서 확인. 부하 클라이언트의
  half-close·EOF 정리와 정리 실패 보고를 추가하고
  [패킷 증거와 수정 전후 결과](performance/2026-09-08-client-close/README.md) 기록.
  과거 1건의 정확한 errno를 소급 확정한 것은 아님

- 2026-09-08: 외부 IPv4 서버 대상 부하 모드와 서버 통계 미수집 표시 추가.
  운영 서버·부하 생성기의 프로세스와 CPU 집합을 분리한 클라이언트 측정
  6회 성공. 서버 종료 이유와 지연을 포함한
  [분리 측정 결과](performance/2026-09-08-external-target/README.md) 기록

- 2026-09-07: 지속 부하의 client별 송신 상한·방별 응답 대기 상한·제한 시간과
  수신 표본 상한 추가. 약 30초의 정상·다중 방·느린 client 측정 9회 성공,
  [원시 결과와 해석 한계](performance/2026-09-07-sustained-load/README.md) 기록

- 2026-09-07: 서버 연결 종료 이유와 부하 도구 단계별 실패 원인을 계측해
  큰 burst의 세션 대기 이벤트 상한 초과를 확인. 거절 뒤 연결 종료 이벤트가
  유실되어 사용자·방 상태가 남는 결함을 수정하고
  [재현 결과](performance/2026-09-07-disconnect-diagnostics/README.md) 기록

- 2026-09-07: 고정 Linux 컨테이너에서 정상·느린 클라이언트 35회와
  마이크로벤치마크 반복 측정, 큰 burst 실패를 포함한
  [예비 기준값](performance/2026-09-07-baseline/README.md) 기록.
  ChatFanout의 협상 준비와 실패 종료 코드 회귀 검사 보강

- 2026-09-07: 서버·Qt·콘솔·부하 도구에 연결별 버전 협상과 클라이언트의
  5초 협상 제한 시간을 추가하고, 실패 시 오류 송신 후 연결 종료 적용

- 2026-09-07: Qt 로그를 최근 1,000개로 제한하고 소켓 내부 버퍼를 포함한
  송신 대기 상한 1 MiB 및 초과 시 복구 가능한 전송 오류 적용
- 2026-09-04: Qt 클라이언트에 로그인·방 작업 요청의 진행 상태를 추가해
  응답 대기 중 중복 요청과 채팅 전송을 차단하고 관련 입력을 비활성화
- 2026-09-03: 구조화 문자열 value의 percent encoding, UTF-8/control 검증과
  고정 byte 상한을 protocol·server·Qt·콘솔·부하 도구에 함께 적용
- 2026-09-03: 시작·실패·종료 구조화 로그와 설정 가능한 주기 및 최종
  과부하 snapshot 출력을 추가하고 민감한 사용자·DB 값은 제외
- 2026-08-27: PostgreSQL 사용자 저장소와 영구 UUID 로그인을 운영 서버에
  연결하고 DB 작업 중 worker 비차단 및 실패 세션 격리 적용
- 2026-08-27: worker handler 예외를 실패 세션에 격리하고 연결 종료와
  `handler_exceptions` 통계로 관측
- 2026-08-26: `ServerConfig` 전체 값의 유효 범위 검증과 설정 문서 보강
- 2026-08-25: `SIGINT`와 `SIGTERM`을 기존 정상 종료 drain 경로에 연결
- 2026-08-20: 반복 로그인을 거부하고 명시적 퇴장 뒤에만 방 생성·참가 허용
- 2026-08-20: 동일한 방 재참가를 명시적 오류로 처리하고 기존 방 상태 보존
- 2026-08-20: Qt 전송 오류와 연결 상태를 일치시키고 실패한 채팅 입력 보존
- 2026-08-20: 실제 서버 시나리오의 deadline 회귀 테스트를 성공한 전송에
  기반한 결정적 조건으로 변경하고 sanitizer 반복 실행 안정성 확보

## 1차 완료 범위

초기 개발 계획은 완료했습니다. 현재는 기존 기능을 검증하고 결과를 전달할 수
있게 정리하는 마무리 단계입니다. 아래 세 항목을 완료하면 1차 버전을 확정합니다.
후속 성능 연구나 기능 확장을 이 단계의 필수 조건으로 계속 추가하지 않습니다.

### 1. 확인된 TCP 지연 개선 적용·검증

- [ ] 서버가 수락한 TCP 연결에 TCP_NODELAY 적용 및 회귀 테스트 추가
- [ ] 같은 환경·인자로 변경 전후의 지연, 패킷 수와 서버·클라이언트 CPU 비용 비교
- [ ] 정상 단일 방·다중 방의 메시지 정확성과 slow-client 격리 검증
- [ ] 관련 build/test, format-check, tidy-check, sanitizer 및 CI 통과

약 40ms ACK 대기 개선과 정확성 회귀 여부를 확인하고 비용·한계를 기록합니다.
남은 p99를 모두 해명하거나 임의의 절대 지연 목표를 달성하는 것은 완료 조건이
아닙니다. 확인된 정확성·자원 해제 회귀가 있으면 해결한 뒤 다음 단계로 갑니다.

### 2. 최종 결과와 사용 문서 정리

- [ ] README에서 구현 범위, 실행·데모, 구조·설계 이유, 테스트와 측정 결과로 연결
- [ ] 실제로 발견한 결함과 해결 근거, TCP 개선 전후 결과를 간결하게 요약
- [ ] 구현되지 않은 기능과 측정하지 않은 범위를 명시하고 후속 과제로 분리

### 3. 최종 실행 확인과 버전 확정

- [ ] 문서의 시작 절차로 서버·DB를 실행하고 로그인·방 참가·채팅·연결 종료 데모 확인
- [ ] Qt 클라이언트의 빌드·테스트와 기존 데모 절차 확인
- [ ] 최종 main CI 통과, 작업용 환경 정리 및 완료 상태 기록
- [ ] 검증한 최종 커밋에 1차 완료 태그 `v0.1.0` 게시

위 세 항목을 마치면 1차 완료로 선언합니다. 새 기능이나 추가 최적화는
이 버전을 기준으로 별도 작업으로 진행합니다.

## 1차 완료 이후 과제

다음은 현재 알려진 한계와 확장 방향이며 1차 완료의 필수 조건이 아닙니다.

- TCP 설정 변경 뒤 남은 지연의 queue·스케줄링·클라이언트 구간별 계측
- 더 큰 부하·방 수와 다른 물리 머신에서 처리량·지연·mutex 경합 재측정
- 최대 처리량, 장시간 안정성 및 장기 회귀 기준 확립
- 측정 근거가 확보될 때 RoomService 잠금 분할, shard와 TcpServer 책임 분리 검토
- Qt 기능 확장, TLS·인증, 영구 방·채팅 기록 등은 [로드맵](roadmap.md)에서 검토

## 검증 기준

변경은 관련 회귀 테스트를 먼저 추가하고 다음 검증을 통과해야 합니다.

```bash
cmake --preset core-dev
cmake --build --preset core-dev
ctest --preset core-dev
```

Linux 네트워크 변경은 `linux-dev`, Qt 변경은 `qt-client-dev` preset으로
추가 검증합니다. 완료 전 `format-check`와 가능한 경우 `tidy-check`를
실행합니다.

## 문서 사용 규칙

- 완료 여부와 지금 착수할 작업의 순서는 이 문서에서만 관리합니다.
- 재현 가능한 열린 결함과 수정 완료 조건은 `known-issues.md`에서
  관리하며, 해결 이력과 우선순위를 중복해서 기록하지 않습니다.
- 아직 착수하지 않은 중장기 기능과 성능 방향은 `roadmap.md`에서
  관리하며, 현재 작업의 상세 목록을 중복해서 기록하지 않습니다.
- `development-plan.md`는 완료된 초기 개발 계획의 기록입니다.
- `design/`과 `plans/`는 각각 당시 결정과 구현 절차의 기록으로 유지합니다.
